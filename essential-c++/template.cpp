#include <string>
using namespace std;

template<class Type> class stack;
void fi(stack<char>);
class Ex {
    stack<double> &rs;
    stack<int> si;
};

int main()
{
    stack<char> *sc;
    fi(*sc);
    int i = sizeof(stack<string>);
}
